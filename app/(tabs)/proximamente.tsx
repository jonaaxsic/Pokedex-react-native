// Dummy file required by Expo Router for the "proximamente" tab.
// This tab never navigates - it shows a ComingSoon toast instead.
import { View } from 'react-native';

export default function ProximamenteScreen() {
  return <View />;
}
